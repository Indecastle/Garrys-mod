--@name texture
--@author Person8880
--@shared

if SERVER then
    wire.adjustOutputs( { "Texture" }, { "string" } )
else
    local Text = "Bleh."

    local m = render.createMaterial("models/props_junk/woodcrates01a")
    //local t = m:getTexture()
    
    wire.ports.Texture = "models/props_junk/woodcrates01a"
end

